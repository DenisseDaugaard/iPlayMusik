export default function PageTitle({title}){
    return(
        <h1 className="mb-12 text-5xl font-extrabold bg-gradient-to-r from-pink-500 to-orange-300 bg-clip-text text-transparent">{title}</h1>
    )
}